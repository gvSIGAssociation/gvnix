<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<configuration>
  <gvnix>
    <menu>
    <!-- DISID No dependencies needed -->
    <!-- 
    <dependencies>

      <!-#- Ivy-style dependency definition -#->
      <dependency org="org.gvnix" name="annotations" 
        rev="${gvnix.version}" />

      <!-#- Maven-style dependency definition (should be used if exclusions 
        need to be defined) -#-> 
      <dependency>
        <groupId>org.gvnix</groupId>
        <artifactId>org.gvnix.annotations</artifactId>
        <version>${gvnix.version}</version>
      </dependency>

    </dependencies>
    -->

    <properties>
      <gvnix.version>${project.version}</gvnix.version>
    </properties>

    </menu>
  </gvnix>
</configuration>
