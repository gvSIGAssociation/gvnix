
// SLIDE 5: Generación de código

// Consultamos filtros disponibles sobre Productos en función del precio
finder list --class ~.domain.Producto --filter precio

// Añadir un buscador de Productos cuyo precio esté entre dos valores  
finder add --finderName findProductoesByPrecioBetween --class ~.domain.Producto

// Añadir un buscador de pedidos por total y cliente
finder add --finderName findPedidoesByTotalGreaterThanEqualsAndCliente --class ~.domain.Pedido

backup
