<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
        <!-- TOREMOVE This is a temporal wrapping until String roo
		includes it -->
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.roo</groupId>
		<artifactId>wrapping</artifactId>
		<version>1.1.5.RELEASE</version>
	</parent>
    <groupId>org.springframework.roo.wrapping</groupId>
	<artifactId>${project.groupId}.${pkgArtifactId}</artifactId>
    <version>${osgiVersion}</version>
	<packaging>bundle</packaging>
	<name>Spring Roo - Wrapping - ${pkgArtifactId}</name>
    <description>This bundle wraps the standard Maven artifact: ${pkgArtifactId}-${pkgVersion}.</description>
    <repositories>
        <repository>
            <!-- There is no maven repo with required javaparser version.
                 So, we store it (temporally) in gvNIX repo -->
            <id>maven.gvnix</id>
            <name>gvNIX googlecode maven repository</name>
            <url>https://gvnix.googlecode.com/svn/repo/</url>
        </repository>
    </repositories>
    <properties>
        <pkgArtifactId>javaparser</pkgArtifactId>
        <pkgVersion>1.0.9</pkgVersion>
        <osgiVersion>${pkgVersion}.0010</osgiVersion>
        <pkgVendor>JavaParser Project</pkgVendor>
        <pkgDocUrl>https://github.com/matozoid/javaparser/</pkgDocUrl>
        <pkgLicense>http://www.gnu.org/licenses/lgpl.html</pkgLicense>
    </properties>

    <dependencies>
        <dependency>
            <groupId>japa.javaparser</groupId>
            <artifactId>${pkgArtifactId}</artifactId>
            <version>${pkgVersion}</version>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>
